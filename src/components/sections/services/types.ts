import type { IconType } from "react-icons";

export interface Service {
  icon: IconType;
  title: string;
  description: string;
  color: string;
  hoverColor: string;
  bgGradient: string;
  delay: string;
  actionText: string;
}
